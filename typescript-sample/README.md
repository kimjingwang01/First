# TypeScript Sample Project

A modular TypeScript sample project with CLI, domain services, repository layer, and tests.

## Run

```bash
cd typescript-sample
npm install
npm run build
npm start -- --mode normal --items 20 --seed 7
```

## Test

```bash
npm test
```
